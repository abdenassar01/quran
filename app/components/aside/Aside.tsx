import { NavLink } from "@remix-run/react"

 
function Aside() {
  return (
    <main className="aside-wrapper">
        <NavLink to={ `/quran/002` } className="item">Text</NavLink>
        <NavLink to={ `/quran/003` } className="item">Text</NavLink>
        <NavLink to={ `/quran/004` } className="item">Text</NavLink>
        <NavLink to={ `/quran/005` } className="item">Text</NavLink>
        <NavLink to={ `/quran/006` } className="item">Text</NavLink>
        <NavLink to={ `/quran/007` } className="item">Text</NavLink>
        <NavLink to={ `/quran/008` } className="item">Text</NavLink>
        <NavLink to={ `/quran/009` } className="item">Text</NavLink>
        <NavLink to={ `/quran/010` } className="item">Text</NavLink>
        <NavLink to={ `/quran/020` } className="item">Text</NavLink>
        <NavLink to={ `/quran/021` } className="item">Text</NavLink>
        <NavLink to={ `/quran/022` } className="item">Text</NavLink>
        <NavLink to={ `/quran/023` } className="item">Text</NavLink>
        <NavLink to={ `/quran/024` } className="item">Text</NavLink>
        <NavLink to={ `/quran/025` } className="item">Text</NavLink>
        <NavLink to={ `/quran/002` } className="item">Text</NavLink>
        <NavLink to={ `/quran/003` } className="item">Text</NavLink>
        <NavLink to={ `/quran/004` } className="item">Text</NavLink>
        <NavLink to={ `/quran/005` } className="item">Text</NavLink>
        <NavLink to={ `/quran/006` } className="item">Text</NavLink>
        <NavLink to={ `/quran/007` } className="item">Text</NavLink>
        <NavLink to={ `/quran/008` } className="item">Text</NavLink>
        <NavLink to={ `/quran/009` } className="item">Text</NavLink>
        <NavLink to={ `/quran/010` } className="item">Text</NavLink>
        <NavLink to={ `/quran/020` } className="item">Text</NavLink>
        <NavLink to={ `/quran/021` } className="item">Text</NavLink>
        <NavLink to={ `/quran/022` } className="item">Text</NavLink>
        <NavLink to={ `/quran/023` } className="item">Text</NavLink>
        <NavLink to={ `/quran/024` } className="item">Text</NavLink>
        <NavLink to={ `/quran/025` } className="item">Text</NavLink>
    </main>
  )
}

export default Aside